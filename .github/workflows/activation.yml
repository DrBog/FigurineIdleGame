name: Get Unity License File
on: [workflow_dispatch]
jobs:
  activation:
    name: Request License Matrix
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: game-ci/unity-request-activation-file@v4
        with:
          targetPlatform: WebGL
